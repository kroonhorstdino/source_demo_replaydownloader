
get dem file
parse dem file to csv

Put csv in Dataset

Use deaths as labels for network (later something else)

load batches with dataloader from Dataset
    get balanced batches for each player(?)
    only compute loss for one player at a time


use simple feed forward architecture
use shared network architecture

calculate loss and accuracy
plot with sklearn or tensorboard

get results
